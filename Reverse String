import java.lang.*;
import java.io.*;
import java.util.*;
public class Hello{
    public static void main(String args[])
    {
       Scanner sc=new Scanner(System.in);
      String s=sc.nextLine();
      StringBuilder scyu=new StringBuilder(s);
      System.out.println(scyu.reverse());
    }
}
